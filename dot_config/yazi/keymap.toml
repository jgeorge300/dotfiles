# With the Zsh y wrapper: q stays in the original directory, Q changes it.
[[mgr.prepend_keymap]]
on = "q"
run = "quit --no-cwd-file"
desc = "Quit without changing directory"

[[mgr.prepend_keymap]]
on = "Q"
run = "quit"
desc = "Quit and change shell directory"
